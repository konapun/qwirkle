package state_test

import (
	"testing"
)

func TestEventAggregator_Publish(t *testing.T) {
	// TODO
	//  manager := NewManager(NewState())
	// aggregator := NewEventAggregator(manager)
	//
	// // Test that no observers are called when no events are published
	// observerCalled := false
	// observer := state.NewRuntimeObserver(func(event *Event) {
	// 	observerCalled = true
	//
	// 	require.Equal(t, EventTypePlayerAdded, event.Type)
	// 	require.Equal(t, 1, event.PlayerIndex)
	// })
	// aggregator.Subscribe(observer)
	// require.False(t, observerCalled)
	//
}
