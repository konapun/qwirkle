package input

// The DSL package provides a domain-specific language for defining input commands.
// The idea is to represent a level of abstraction above the input package, allowing
// for a more expressive way to define input commands.
/*
addPlayer := dsl.addPlayer()

placeTiles := dsl.PlaceTiles().WithTileRun(
  dsl.TileRun().WithTiles(
    dsl.Tile().WithColor("red").WithShape("circle"),
    dsl.Tile().WithColor("red").WithShape("star"),
  ),
)
*/
